name : Scheduled Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello HeLLO"; date
